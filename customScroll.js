// const customScroll = document.querySelector('.custom-scroll');
// const content = customScroll.querySelector('.content');
// let isScrolling = false;

// customScroll.addEventListener('scroll', () => {
//   isScrolling = true;
// });

// setInterval(() => {
//   if (isScrolling) {
//     isScrolling = false;
//     const scrollPercent = customScroll.scrollTop / (customScroll.scrollHeight - customScroll.clientHeight);
//     const thumbHeight = customScroll.clientHeight * scrollPercent;
//     const thumb = customScroll.querySelector('.thumb');
//     thumb
